'use client';\n\nimport { useState } from 'react';\nimport { usePathname } from 'next/navigation';\nimport Link from 'next/link';\n\nconst menuItems = [\n  { id: 'dashboard', label: 'Dashboard', icon: '📊', href: '/dashboard' },\n  { id: 'projects', label: 'Projects', icon: '📁', href: '/dashboard/projects' },\n  { id: 'tasks', label: 'Tasks', icon: '✓', href: '/dashboard/tasks' },\n  { id: 'calendar', label: 'Calendar', icon: '📅', href: '/dashboard/calendar' },\n  { id: 'budget', label: 'Budget', icon: '💰', href: '/dashboard/budget' },\n  { id: 'reports', label: 'Reports', icon: '📈', href: '/dashboard/reports' },\n  { id: 'beneficiaries', label: 'Beneficiaries', icon: '👥', href: '/dashboard/beneficiaries' },\n  { id: 'documents', label: 'Documents', icon: '📄', href: '/dashboard/documents' },\n  { id: 'partners', label: 'Partners', icon: '🤝', href: '/dashboard/partners' },\n  { id: 'messages', label: 'Messages', icon: '💬', href: '/dashboard/messages' },\n  { id: 'settings', label: 'Settings', icon: '⚙️', href: '/dashboard/settings' },\n];\n\nexport function Sidebar({ isOpen, setIsOpen }) {\n  const pathname = usePathname();\n\n  return (\n    <>\n      {/* Mobile overlay */}\n      {isOpen && (\n        <div\n          className=\"fixed inset-0 bg-black/50 lg:hidden z-30\"\n          onClick={() => setIsOpen(false)}\n        />\n      )}\n\n      {/* Sidebar */}\n      <aside\n        className={`fixed left-0 top-0 h-screen w-64 bg-white dark:bg-gray-900 border-r border-gray-200 dark:border-gray-800 flex flex-col transition-transform lg:translate-x-0 z-40 ${\n          isOpen ? 'translate-x-0' : '-translate-x-full'\n        }`}\n      >\n        {/* Logo */}\n        <div className=\"p-4 border-b border-gray-200 dark:border-gray-800\">\n          <Link href=\"/dashboard\" className=\"flex items-center gap-3\">\n            <div className=\"w-10 h-10 bg-green-primary rounded-lg flex items-center justify-center text-white font-bold text-sm\">\n              ENA\n            </div>\n            <div className=\"text-sm font-semibold text-gray-900 dark:text-white\">\n              ENGAGE<br />AFRICA\n            </div>\n          </Link>\n        </div>\n\n        {/* Navigation */}\n        <nav className=\"flex-1 overflow-y-auto p-3 space-y-1\">\n          {menuItems.map((item) => {\n            const isActive = pathname === item.href;\n            return (\n              <Link\n                key={item.id}\n                href={item.href}\n                onClick={() => setIsOpen(false)}\n                className={`flex items-center gap-3 px-3 py-2.5 rounded-lg transition ${\n                  isActive\n                    ? 'bg-green-light dark:bg-green-900 text-green-primary font-medium'\n                    : 'text-gray-500 dark:text-gray-400 hover:text-gray-800 dark:hover:text-gray-200'\n                }`}\n              >\n                <span className=\"text-lg\">{item.icon}</span>\n                <span className=\"text-sm\">{item.label}</span>\n              </Link>\n            );\n          })}\n        </nav>\n\n        {/* Promo */}\n        <div className=\"p-3 bg-green-primary text-white rounded-lg m-3\">\n          <div className=\"text-2xl mb-3\">🌍</div>\n          <p className=\"text-xs leading-relaxed mb-3\">Empowering communities. Building a better future.</p>\n          <button className=\"w-full px-3 py-1.5 bg-white/20 hover:bg-white/30 rounded text-xs font-medium transition\">\n            Learn More\n          </button>\n        </div>\n      </aside>\n    </>\n  );\n}\n